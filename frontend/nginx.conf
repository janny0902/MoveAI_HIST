server {
    # gateway nginx → frontend:8080  / 구이미지·헬스체크 호환용 80
    listen 8080;
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
